build/
*.o
*.elf
*.bin.tmp
.cache/
.venv/
sim/sim
sim/build/
compile_commands.json
sim/compile_commands.json
docs/doxygen/
.clangd
.cache/

# WireGuard private key + peer secrets — never commit, never sync to git mirror.
# Copy wireguard_secrets.h.example -> wireguard_secrets.h and fill locally.
wireguard_secrets.h
